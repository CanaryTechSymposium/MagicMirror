<UserControl
    x:Class="MagicMirror.CountDownTimer.CountDownTimer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagicMirror.CountDownTimer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Heading}" Foreground="White" FontSize="36" HorizontalAlignment="Center" />
            <TextBlock Text="{Binding Duration}" Foreground="White" FontSize="20" HorizontalAlignment="Center" />
            <TextBlock Text="{Binding WorkingDaysDuration}" Foreground="White" FontSize="20" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
